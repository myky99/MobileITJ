<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:MobileITJ.Models"
             xmlns:viewmodel="clr-namespace:MobileITJ.ViewModels"
             x:DataType="viewmodel:ViewMyJobsViewModel"
             x:Class="MobileITJ.Views.Customer.ViewMyJobsPage"
             Title="View My Jobs"
             BackgroundColor="{StaticResource BackgroundColor}"
             x:Name="ThisPage">

    <RefreshView Command="{Binding LoadJobsCommand}" 
                 IsRefreshing="{Binding IsBusy}">
        <CollectionView ItemsSource="{Binding Jobs}"
                        EmptyView="You have not posted any jobs.">

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:CustomerJobDetail">
                    <Frame Padding="15" Margin="10,5" CornerRadius="10" BorderColor="#E0E0E0" HasShadow="True">
                        <Grid RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto" ColumnDefinitions="*, Auto">

                            <Label Grid.Row="0" Grid.Column="0" 
                                   Text="{Binding Job.Title}" 
                                   FontAttributes="Bold" FontSize="18" TextColor="{StaticResource PrimaryColor}"/>

                            <Label Grid.Row="0" Grid.Column="1" 
                                   Text="{Binding Status}" 
                                   FontSize="12" TextColor="Gray" VerticalOptions="Start"/>

                            <Label Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                                   Text="{Binding Job.JobDescription}" 
                                   FontSize="14" Margin="0,2,0,0"/>

                            <Label Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" 
                                   Text="{Binding Job.Location}" 
                                   FontSize="14" Margin="0,5,0,0" TextColor="Gray"/>

                            <Label Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                                   FontSize="16" FontAttributes="Bold" TextColor="{StaticResource PrimaryColor}" Margin="0,10,0,0">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="Rate: "/>
                                        <Span Text="{Binding Job.RatePerHour, StringFormat='{0:C}/hr'}" />
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>

                            <Label Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                                   Text="{Binding SlotsDisplay}"
                                   FontAttributes="Italic"
                                   TextColor="Gray"
                                   FontSize="14" Margin="0,5,0,0" />

                            <HorizontalStackLayout Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Spacing="10" Margin="0,15,0,0">

                                <Button Text="Workers"
                                        Style="{StaticResource TopTabButton}"
                                        BackgroundColor="{StaticResource SecondaryColor}"
                                        TextColor="White"
                                        HeightRequest="40"
                                        Command="{Binding BindingContext.ViewApplicationsCommand, Source={x:Reference ThisPage}}"
                                        CommandParameter="{Binding .}"/>

                                <Button Text="Complete"
                                        Style="{StaticResource TopTabButton}"
                                        BackgroundColor="Green"
                                        TextColor="White"
                                        HeightRequest="40"
                                        Command="{Binding BindingContext.CompleteJobCommand, Source={x:Reference ThisPage}}"
                                        CommandParameter="{Binding .}"
                                        IsVisible="{Binding CanCompleteJob}"/>

                                <Button Text="Failed"
                                        Style="{StaticResource TopTabButton}"
                                        BackgroundColor="Red"
                                        TextColor="White"
                                        HeightRequest="40"
                                        Command="{Binding BindingContext.MarkIncompleteCommand, Source={x:Reference ThisPage}}"
                                        CommandParameter="{Binding .}"
                                        IsVisible="{Binding CanCompleteJob}"/>
                            </HorizontalStackLayout>

                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>
    </RefreshView>
</ContentPage>