<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:MobileITJ.Models"
             xmlns:viewmodel="clr-namespace:MobileITJ.ViewModels"
             x:DataType="viewmodel:ViewAvailableJobsViewModel"
             x:Class="MobileITJ.Views.Worker.ViewAvailableJobsPage"
             Title="Available Jobs"
             BackgroundColor="{StaticResource BackgroundColor}"
             x:Name="ThisPage">

    <RefreshView Command="{Binding LoadJobsCommand}" 
                 IsRefreshing="{Binding IsBusy}">
        <CollectionView ItemsSource="{Binding AvailableJobs}"
                        EmptyView="No jobs are currently available.">

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Job">
                    <Frame Padding="15" Margin="10,5" CornerRadius="10" BorderColor="#E0E0E0" HasShadow="True">
                        <Grid RowDefinitions="Auto, Auto, Auto, Auto, Auto" ColumnDefinitions="*">

                            <Label Grid.Row="0" 
                                   Text="{Binding JobDescription}" 
                                   FontAttributes="Bold" FontSize="18"/>

                            <Label Grid.Row="1"
                                   Text="{Binding Location}" 
                                   FontSize="14" Margin="0,5,0,0"/>

                            <Label Grid.Row="2" 
                                   Text="{Binding SkillsNeeded, Converter={StaticResource SkillsListToStringConverter}}"
                                   FontSize="14" FontAttributes="Italic" TextColor="Gray" Margin="0,5,0,0"/>

                            <Label Grid.Row="3"
                                   FontSize="16" FontAttributes="Bold" TextColor="{StaticResource PrimaryColor}" Margin="0,10,0,0">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="Rate: "/>
                                        <Span Text="{Binding RatePerHour, StringFormat='{0:C}/hr'}" />
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>

                            <Button Grid.Row="4" 
                                    Text="Apply for this Job"
                                    Style="{StaticResource TopTabButton}"
                                    BackgroundColor="{StaticResource SecondaryColor}"
                                    TextColor="White"
                                    Margin="0,15,0,0"
                                    HorizontalOptions="Start"
                                    Command="{Binding BindingContext.ApplyJobCommand, Source={x:Reference ThisPage}}"
                                    CommandParameter="{Binding .}"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>
    </RefreshView>
</ContentPage>